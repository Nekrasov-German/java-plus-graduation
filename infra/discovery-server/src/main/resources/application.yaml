server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
#  server:
#    enable-self-preservation: false  # отключает режим самозащиты (для тестов)
#    eviction-interval-timer-in-ms: 10000  # интервал очистки неактивных сервисов (10 сек)